language: python

os:
- linux

python:
- "3.6"

before install:
- sudo apt install python3-rtree
- pip3 install Cython

install:
- bash install-proto.sh
- pip3 install .

script:
- pytest tests/

addons:
  homebrew:
    packages:
      spatialindex